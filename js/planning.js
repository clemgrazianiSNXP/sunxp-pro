/* Module Planning — gestion du planning des chauffeurs */

function initPlanning() {
  const stationId = window.getActiveStationId ? window.getActiveStationId() : null;
  console.log('Module Planning initialisé — station:', stationId);
}
